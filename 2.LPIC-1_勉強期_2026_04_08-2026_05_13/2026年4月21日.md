・勉強時間
	8h
・内容
	LPIC 1 101 
	ping-t 全部やり切り
	未254→未0 
	過去のミス問題200問近く
・学び
	file はatimeのみ更新
	m,ctimeは内容変更でどっちも変わる 
		ブロック割り当て変わる→メタ情報変わる→ctime変わる
	od -o -x 16 -c ASCII
	ps axで全プロセス uはユーザー表示なだけ
	-ef eは全プロセス fはフォーマット 
	vi
		:!コマンド
		新規ファイルは保存時にファイル名に聞かれる
		:r ファイル名　ファイルをカレント行に読み込ませる
		X|x
		:set nu|number
		:%s/A/B/g 
	fsck コマンド確認
		-t type
		-A /etc/fstabに書いたる奴check
		-N 実行しない
		-a 自動修復
		-r 対話修復
	rpm パッケージファイル名指定(.rpm)
	まだインストールされてないローカルにある物理ファイル
	-i -U -F -qp 
	未インス、更新はファイル名
	インス済みはパッケ名
	rpm --changelog
	-c 設定ファイル一覧
	インストールされるとは
		実行ファイルが/usr/binに置かれる
		設定ファイルが/etc
		ドキュメントが/usr/share/man追加
		RPMDB更新 rpm -qに認識される
		依存関係解決
		ファイルからシステムの一部になる
	ls -a隠し -A . ..見える -R再帰
	Upstart 
		SysVinitの改良版
		サービスとデーモンまとめてジョブ
		イベント駆動でジョブ実行
		並列処理
	fdisk コマンド整理
		m menu メニュー出す
		p pring 出力
		q quit 変更保存x終了
		w write 変更保存o終了
		n new 新しいパーティション作る
		d delete 既存パーティション削除	
		t type パーティションシステムID変更
		l list 選択可能なパーティション一覧
		v verify パーティションの整合性
		u unit 表示単位
	touch タイムスタンプ(mtime)全部変わる
	mv -i interract
	cpio cp in and out
		cpio -o ファイルからアーカイブ作成
			-i アーカイブからファイル展開
	tune2fs -c 10 10回したらfscheck入る！
	xzcat 展開してstdout
	fg jobs + ついてるジョブは数字引数要らない
	/* は直前0回以上繰り替えし
	graphical.targetはサーバーだといらないリソース無駄
	runlevel(num).targetあるよ
	SysVinit init q telinit Qとかで /etc/inittab設定ファイル再読み込み
	zypper 
		list-updates lu
		repos|lr
		refresh
	シェル変数 $いらないよ
	makewhatis whatisDBをつくる！
	find . でカレントディレクトリのパスリストアップ その後パイプ渡すテクニック
	cpio ファイル受け取れない
	LVMについて
		PV(物理ボリューム)
		PV集めてVG(ボリュームグループ)作る (仮想ディスク)
		VGから切り出してLV(論理ディスク)作る(仮想パーティション)
		物理的にバラバラなのをまとめあげて必要分だけ取り出す！
		→物理的並びを意識しなくてすむ！フラグメンテーション解決
		シーク自体はやらないといけない
		文字列送り<<< ヒアストリング
	/var/log/messages
	yum 
		list インスしたかどうか
		grouplist
		check-update_ sないよ！
	set env 
		printenvは個別の変数きける
	cp 
		-f force 上書き　元の削除
		-i interract 対話
		-p preserve ファイル属性をコピー
		-r| -R ディレクトリ再帰コピー
	dpkg
		-E 同じverインスされてたらインスしない
		-G 新しい"
		dpkg-reconfig　インスしたときと同じ再設定
		-l --list (パターン)　インス済みのパック検索
		-L --List 指定のパッケからインスされたファイルのパス一覧
			パッケからファイル一覧
	tar -f (アーカイブ名) 元ファイル の順
	IRQ /proc/interrupts
	共有ライブラリパス LD_LIBRARY_PATH
	singleモード rescue.target runlevel1.target 
		single.targetないぞ！
	GRUB
		kernel カーネルイメージ
	pvcreate->vgcreate->lvcreate
	uniq
		重複してるの1行にまとめる他のそのまま
		-d duplicate 重複した行のみ抜き出し
		-u uniq 重複していない唯一行のみ抜き出し
		-c count 各行の出現回数を先頭に表示
		-i ignore 大小文字無視
	セクション
		1 ユーザーコマンド
		8 システムコマンド
	rpm2cpio rpm->cpio 2はto
	ロードされてるモジュール
		lsmod cat /proc/modules
	ACMについて
	tail 10行デフォ
	tar 
		-z gzip 
		-j bzip bとjにてる
		-J xz 強そう
	mke2fs mkfs
	chown . :グループ　単品でもおけ
	exFAT MS作ったFS
	tee -a append >>上書きする
	正規表現は最も左から最も長く一致するものにマッチ
	fdisk MBR 
	gdisk GPT
	parted 両対応
		HISTFILE historyパス
	cloud-init
		クラウドインスタンスの構成とかに使うLinuxパッケージ
	正規表現
		特殊文字 /
		文字クラス []
		数量詞 ** +
		アンカー ^ $
	systemctl reboot メッセ残せない
	e2fsck -p 自動修復
	dd count
	rpm --verify -Vは -a -allか --nomd5のみ
	sha256sum -c -check
	systemctl list-unit-files
	udev /etc/udev/rule.dに配置
		10-local...../.rules
		複数管理がよき
		数字の若い方から優先順位
	xargs はstdinを自身の引数じゃなくコマンドの引数にする
	- ハイフンはstdinをファイル名に指定するけどコマンドが定めている
	gzip -c stdoutにする パイプには必須！
・感想
	ping-tほぼほぼやり切り
	ミス分も復習してping-tの問題はほぼ解けるレベルに仕上げた
	良い集中でいい感じに頑張れた
・次やること
	申し込み
	あずき本、白本、模試
	復習さらえる