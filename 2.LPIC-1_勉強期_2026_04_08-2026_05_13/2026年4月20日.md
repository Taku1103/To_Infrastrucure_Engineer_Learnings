・勉強時間
	6h
・内容
	LPIC 1 101 ping-t 226問　未254まで
・学び
	split 1000 defo
	cd コマンド実行したユーザー
	tail -f follow 
	split 勝手に末尾aa abつく
	nohup コマンド
	stat
	sed 一個でも
	nice 値指定 プロセス実行
	ディレクトリパーミッション
		r ディレクトリ内内容参照可 ls find
		w ディレクトリ内 作成削除リネーム可能
		x ディレクトリ内移動可能 cd
	. = source sh実行権なくても動く
	bash 子プロセス起動
	レスキューディスク
		/var/log見ろ　起動ミスったときは
	ロードするカーネルに合体させること
	デバイスファイルは窓口
	tune2fs -cマウント回数 -i 時間間隔
	HDD 容量当たりコスパヨシ
	pgrep -u 実効ユーザー euid　権限のユーザー
	-U 実ユーザー 実行したユーザー
	パーティションタイプ ID
		82 swap
		83 linux データ領域
	ホットプラグデバイス udev
	コールドプラグデバイス NIC
	xfs_check →xfs_repair
	BIOS 起動デバイス順序　
	yum 設定ファイル /etc/yum.repos.d
	mountオプション 
		user 一般ユーザーマウント可　アンマウント本人のみ
		users 誰でもアンマウント可
		nouser 一般不可
	シンボリックリンクのパーミッションはリンク先を変えることになる
	apt-get -f install で修復インストール
	sda sdb ディスク番号 sda1 2 3 パーティション番号
	tar -f ファイル アーカイブ名
	type which PATH参照 whereis はしない
	screen tmux
	tar -z gzip通す
	- 標準入力を指す|とかの前コマンドを-fの引数に取るとか
	OS 起動できない→レスキューモードでHDDのログ除く　再起で消えるログある
	systemctlのコマンド reboot システム再起　restartサービス再起　違い
	set-default get-default 次回起動時ターゲット設定/確認
	シングルクォーテーションはすべて文字ダブルは基本文字で一部扱う $,``,\
	set 環境変数、シェル変数 確認コマンド 
	set オプション シェルのオプション設定
	grub>数字でランレベル設定
	umount -> busy ユーザー使用中orプロセス中
	sedはシングルクォーテーション使うの必須　
	sed内で`\`処理する　正規表現拾う
	yum groupinstall
	RAID上の構成
	journalctl -k kernel --dmesg= dmesg
	chown 指定パー　ファイルのパー指定要らない
	cut -c 文字 -f フィールド -d 区切り
	sort -t 区切り -n 数としてみる -k ソート対象フィールド key -r reverse
	ln リンク元 ->リンク
	先にdefault.target rmして消して
	default.targetとして graphical.targetのシンボリックリンク作る！
	選択肢 同じ意味のコマンド or 順序だってコマンド
	RAID ハードウェアRAID ソフトウェアRAID
	I-i|a-A
	mount オプションdefault async auto dev rw nouser exec suid
	du -s サブo -S サブx
	free top スワップ領域、メモリ領域
	swapon swapoff スワップ領域オンオフ
	modprobe 設定 /etc/modprobe.d/myconfig.conf
	
・感想
	結構がっつりできた
	大体細かいところも覚えてきて速度感上がってサクサク行けるようになった
・次やること
	pingtやり切る