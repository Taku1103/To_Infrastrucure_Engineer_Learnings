・勉強時間
	4.5h
・内容
	LPIC 102 あずき本 9章全部　管理タスク
・学び
	ユーザー管理とグループ管理
		useradd コマンド
		スケルトンディレクトリ /etc/skel
		グループの追加は usermod -aG で追加する
		-Gだと既存のグループも引数に毎回書く必要ある
		groupdelはプライマリーグループだと消せない
		シャドウパスワード
			一見000に見えるけどroot触れる SUIDついてるから一般でもrw可能
	getent
	ジョブスケジュール
		cron 周期的
			crond crontab 
			/etc/crontab　システム
			/etc/cron.d/ ユーザーのcrontab
			crontab の 実行時間指定はORで繋がってることに注意
				細かいほう(分側)を** で書くと、意図しない実行になる可能性ある
				こういうときは分もきっちり0とか指定して書いておく
			コマンドは絶対パスで！環境変数違う
			実行権限あるかチェック
		at　一度きり
			atd 
		アクセス制御
			cron 
				.allow
				.deny　の順
				どっちもなかったら rootのみor全ユーザーok
			at
				.allow
				.deny　の順（空だったら全ユーザーok）
				どっちもなかったら rootのみ
		systemd
			.timerユニットを使う
			.serviceとセットで使う
			同じ名前だと自動的にsystemdが実行してくれる
			Unit=で実行service指定可能
	ローカライゼーション
		ロケール
		i18n 間に18文字あるから k8sと同じ略し方
		localeコマンド
		文字コード
			UTF-8 国際標準
			iconv で文字コード変換コマンド
		タイムゾーン
			/usr/share/zoneinfoのファイルを/etc/localtimeにシンリンorコピー
			で適用
			TZ環境変数は一時的
			/etc/timezoneも同様
			ハードウェアクロック(RTC)はUTC　ローカル時間はJST
			ローカルタイム=UTS+TZのオフセット
・感想
	丁寧に勉強
	今回はそれほど深く掘った事柄がなかった
	あんまり集中自体は出来なかったかも
	開始時間遅れた　もう少しテンポ上げたい
・次やること
	あずき本 10章