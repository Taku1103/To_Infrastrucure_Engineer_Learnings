・勉強時間
	4h
・内容
	LPIC 1 101 pint-t 100問
・学び
	デバイスクラスの分類
		HID MassStrage ACM Device
	rpm -p パッケ名　インストール前
	fsck e2fsckファイルチェック 問題修復
	tr -d ,tr -s 連続したら1文字に置き換える squeeze
	apt-cache showpkg 被依存パッケ depends パッケが依存してるパッケ
	起動シーケンスのカーネルの仕事
		ハードを認識、制御、ルートファイルシステムのマウント
	nice値 top ps-l l 
	sed -e コマンド複数
	pkill パターン一致　プロセスキル
	pgrep プロセス表示
	tune2fs -j ext2→ext3
	mkfs 新規
	読み書きが多いところはパーティション ファイルが断片化してきて効率下がる
	CRLF ⇔LF の変換 
		Linux内で$は\r\nの\nまで\rは文字扱い　バッファの中の末尾
	IaaS 仮想マシン、仮想ネットワーク、ブロックストレージ
	zypper update up
	set全部シェル変数、環境変数
	env 全部の環境変数
	printenv一部の環境変数
	rpm のみerase 他remove
	/etc/fstab 設計図
		動的操作はmount　すぐ使えるけど再起動で消える
		静的/etc/fstabに書き込む
			書き込んだらデバイス名(UUID) or マウントポイントの片一方だけでマウントする
		-t FSで指定
		-a /etc/fstabにあるやつ乗せる
	xz -d decompress -k keep -l list 圧縮ファイル一覧
	nl -b a 空行　すべて含める cat -n
	nl -b t 空行以外 cat -b
	IDE接続
	renice -u -p
	unit .device .mount .swap .target .service
	mount --bind 深いパスのショトカ作る
	パーティションとファイルシステム　一対一対応
		連続したセクタの範囲の区切り
	MBR 2.2TB GPT 9.4ZB
	dpkg -l インスされたパッケから
	-L 指定されたパッケから
	s status -S search
	-r remove -p purge
	Vi
		/ ファイル末尾へ検索
		? ファイル先頭へ
		n 次
		N次逆
	find -print 検索改行 -print0でnull文字
	bzcatでbz2展開
	yum 全体 /etc/yum.conf
	個別リポジトリ設定 /etc/yum.repos.d
	組み込み変数ある
	e2fsck -p preen 自動修復
	ps aux -ef
	ps -p -l 
	kill PID
	killall プロセス名(コマンド)
	xfs.fsr デフラグ
	apt-get一旦ダウンロードして保管する(/var/cache/apt/archives)
	インストールしても消えない
	apt-clearでその箱整理
ERE	
	? 直前0 or 1 文字
	* 直前の文字の0以上の繰り返し
	. 任意1文字
・感想
	最近この記録の学びの部分のかなり書くのがしんどくなってきた
	少しずつでいいのに
	脳内でやること決めてたら受験日がずっと先延ばしになってる
	完璧など一生来ないのに
・次やること
	ping-t