・勉強時間
	6h
・内容
	LPIC 1 白本 5, 6章
・学び
	groupsコマンド
	/etc/passwd 心臓 rootグループも許すな
	chmod a alluser
	chown ユーザー.(:)グループで一気に変更可能
	chown -R で再帰的変更
	chown -R /tmp/ryo/* と /tmp/ryo/　の違い覚えておく 
		親ディレクトリが変わるか
	SUID
		sudoのパス打ち省略
		sudoの設定ミスるとえらこっちゃ
	SGID
		親ディレクトリと同じグループになる
	スティッキービット
		実行されても削除されない o+t
	パーティションの横の数はハードリンク数
	そのファイルのiノードが刺されてる数
	ファイル名は所詮ラベル
	ln 元 リンク
	ハードリンクはFS(「「パーティション)を超えられない シンボリックリンクは超えれる
	ハードリンクはディレクトリをさせない
	シンボリックリンク
		ソフトのver切り替え
		長いパス
		ストレージ短縮
		ファイル引っ越し後残し
	ハードリンク
		入口増やすイメージ
		効率的なバックアップ
		ファイルの誤操作防止
		一時的なファイル操作
	コピーは片方編集するとき
	設定ファイルはコピー!
	増分バックアップ
	物理デバイスを同じ論理に入れる
	バックアップは別の物理デバイス内でハードリンクしてるだけ
	変更されない分をハードリンク残りコピーでバックアップ
	find -atime access アクセス, mtime modification 変更 ctime change 属性変更
	xargsは受け取った入力をスペースで分割しまくる
		-print0,-0 のコンボ 
	find -path パス はそのパスを検索パターンとしてカレントディレクトリを探してしまう
	locate /etc/updatedb.conf 外す一覧
	updatedb -e 特定のディレクトリ外す
	which PATH変数参照  whereは存在しないぞ
	whereis 色んなのついてくる
	apropos man -k　部分一致
	whatis man -f　完全
	PATH変数以外 find
	Real name , so name, linker name
		/etc/ld.so/confに作ったライブラリ追加
		ld configで/etc/ld.so.cacheに書き込まれる
		プログラム時に/etc/ld.so.cacheを読みこむ
		デフォは/lib,/usr/libを読みこむ
		一時使用　LB_LIBRARY_PATH　優先権
	rpm オプション
		-q を基本組み合わせる
		 -R Require
		 -f file 逆引き
	yum grouplist, yum group install
	rpm -U 更新　新規も
	rpm -F  更新　新規無し
	vhはおまけ
	rpm -e パッケ名
	rpm2cpio cpioアーカイブ化
	yum list installed
	yum check-update yum list-updates
	yum erase, remove
	yum update更新なの全部インス
	yum upgrade updateのすごいばん
	apt update リストだけ更新
	apt upgrade 更新
	zypper remove
	dpkg -L パッケのファイルリスト
		-S search ファイル逆引き
		-C 問題あるやつだけ
	dselect = dpkg インス確認
	dpkg reconfigure インスした気持ちになれ
	apt dist-upgrade 依存関係　いらんの捨てる
	apt のパッケージの場所書いたファイルの置き場所
		/etc/apt/sources.list
	VMとLXCの違い
		VM 重い　凄い 違うOS
		LXC 軽い　同じカーネル
	ベアメタル型、ホスト型
		どこにホストOS乗ってるか
	完全仮想化、準仮想化
	
・感想
	結構章が重くて進みが遅かった
	6章のパッケージ管理のコマンドやオプションが頭から結構抜けてた
	復習すること多い
・次やること
	7章
	ping-t