・勉強時間
	2.5h
・内容
	LPIC 102 白本の問題復習、模擬試験
・学び
	function 現在シェル実行
	emacspeak
	xhostでのアクセス制御無し　クライアントからサーバー　認証無しにアクセスできる
	/usermod -L !
	passwd -l !!
	ntpdate
	ntpdate -q query 正しいかNTPに確認
	lpadmin プリンタ登録削除
	Ghostscript PostScript→非PostScriptに変換
	lpr -p -# -o raw -l
	lp -d -n -o raw
	lpc 対話
	cupsdisable -c cancel -r reason
	cupsreject
	/var/mail
	TCPWrapper使わん Apache系 http
	ssh -p scp -P
	lsof +f マウントポイント
	gpg --edit-key 対話 
	last /wtmp
	who /utmp
	Section "セクション名"
	chronyc makestep 閾値 回数
	/etc/locale.conf LC_ALLはない
	ssh -i 秘密鍵 Identity
	1-1対応は種類の事　鍵
	ssh-agentはローカルプロセスのssh,ssh-addと通信する
		ポート使用しない、ソケット利用 UNIXドメインソケット
・感想
	明日は受験日、キッチリ準備
	結構時間かかったなという印象
	もっと高速で済ませれた感じはするけど仕方ない
・次やること
	キッチリ起きる,準備
	あずき本の模試をやって頭さえさせる
	ノート復習