・勉強時間
	6h
・内容
	LPIC1 白本1-4章
・学び
	PS1,PS2 
	2次プロンプトの概念
	su - 
	./.bash_history
	mkdir -p parents
	tar.gz
	アーカイブして圧縮 は別
	tgzで一緒
	file ファイルの種類 type コマンドの種類
	1>&2 2の出力先に1の出力先にする
	左から順に適用、先に出力先ファイルしてしてあげないとバグる
	grep | するかstdin
	trは引数ファイル指定できない
	cut -d derimiter 
	cut -f field
	/etc/passwd OSに登録している人数
	uniq 重複行削除
	ファイル内検索 grep ファイル名検索 find
	sed のオプション s substitute
	^ [^]は違う
	EUID 実効ユーザー Real UID 実ユーザ
	fgrep fixed
	vim操作
	:!でコマンド操作
	a,A後 i,I前 o,O行
	/ カーソル後ろ検索 ? 前検索
	シェルと子プロセスの流れ
	タイムスライス
	bc
	ジョブはコマンドの塊
	nohupでログアウトしても動く
	CtrlZ STOP CtrlC INT
	killall シグナル名
	/opt インストしたパッケ　パーティション推奨
	LVMについて
	SATAの接続
	fdisk mkfs mount
	スーパーユーザーに5%のブロック
	セクタ　物理　ブロック　論理（OS）
	mkswap
	df -s ディレクトリ総和 -Sサブ含めない総和
	debugfs
	mount --bind ディレクトリ指定
	/etc/fstab マウントするやつら表 mount -a
	dump バックアップコマンド
	UUID デバイスID
	mount = cat /proc/self/mounts
	
	
・感想
	結構スピード感持ちながらやれた
	久しぶりに早起きできて時間的余裕　良かった
	4章は結構速足でやったから抜けあるかも さらえる
	でもあのスピード感良かった
・次やること
	白本5章- 範囲終わらせる