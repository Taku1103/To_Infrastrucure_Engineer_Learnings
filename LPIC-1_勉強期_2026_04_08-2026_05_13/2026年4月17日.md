・勉強時間
	6h
・内容
	LPIC 1 白本 7章　システムアーキテクチャ
・学び
	PCH　周りのアーキテクチャ
	PCI
	IRQ
	ACPI
	MBR,GPT
	エントリにはGUIDが付加される
	CHSとLBA
	GRUBの理解
		grub-install
			実行プログラム設置
		grub-mkconfig
			マニュアル更新
				/etc/default/grub　人用の下書き
				mkconfig↓
				/boot/grub/grub.cfg　PC用の清書
		MBRに仕込んだboot.imgでスイッチしてcore.img本体で受け取る*.modは部品
	BIOSとUEFIのGRUB適用時の違い
		ESPの中のgrubx64.efiを適用 で終わり
	/proc/cmdline ブート時にカーネルに引き渡された引数
	initramfs 大活躍 色々取ってきてくれるやつ
	LKMの概念
		モノリシック
		カーネル空間、ユーザ空間
		System Call
		Ring 0
	dmsg --clear
	SySVinit /etc/inittab
	ロードする デバイスドライバをカーネルに取り込むこと
	telinit q ファイル再読み込み /etc/inittab
	systemd 大活躍
		ソケットアクティベーション 
		D-Bus
		ユニット .servece .target
			target = ランレベル
		Cgroup
	uname
	wall write all
	uptime,w,top
・感想
	かなり時間がかかった
	理解の時間もかけるべき場所だった
	ふんわりとしていたブートの一通り の手順が大分スッキリした
	特にGRUB,BIOS,UEFI,systemd,initramfs,LKM周りはじっくりやってよかった
・次やること
	pingt