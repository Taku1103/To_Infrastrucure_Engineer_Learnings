・勉強時間
	5.5h
・内容
	LPIC 102 白本 2途中-4終わり
・学び
	DEとDM,WMの整理
	chsh設定
	/etc/shadowの!!
	PAM
	usermod -e chage -E
	有効期限周りの設定
	usrmod -L  passwd -l !,!!パス前につく
	-U -uでアンロック
	uid 500未満 デーモン,ディレクトリ
	cron ジョブスケジューラー
	crontabコマンド
	ユーザーのcrontab /var/spool/cron 6つ
	システムのcrontab /etc/crontab 7つ 実行ユーザー名
	at batch
	at -l atq
	at -c atc
	at -d atrm
	at -q キューしてい a-z a優先度高い
	/etc/at.deny allow
	Timerセクション
		mononic timer
		realtime timer 
	date ハードウェアクロック経由せず設定できる
	ntpdate -q
	ntpd.conf 設定
	/etc/ntp.conf
		drifttime
		restrict
		server
	ntp.conf 
		iburstオプション
	chronyd
	chronyc
	rsyslog syslog
	/var/log/messagesにほとんどログ出てる
	system-cat system-journaldに送る
	logger syslogに送る
	/etc/cups/printers.conf
	lpadmin
	lpとlqr
	lr -p lp -d
	cupsdisable 出力停止
	cupsreject 受付拒否
・感想
	
・次やること
	