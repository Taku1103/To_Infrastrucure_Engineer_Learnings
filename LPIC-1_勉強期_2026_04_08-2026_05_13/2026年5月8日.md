・勉強時間
	6h
・内容
	LPIC 102 ping-t 180->368 188問
・学び
	ssh-add コマ
	route -host -net gw netmask
		ip route viaとの違い
	route del default
	DNS
		nameserver
	NTP
		server
	lo もインターフェース扱い
	ifconfig 有効化してるI/F表示
	SMTPのみオープンリレー概念　送信プロトコルなので
	/etc/rsyslog local0-7
	.forward 改行区切り
	date 月時日分 年 秒
	journalctl since ~ until
	logger system-cat どっちもjounrnalに取り込める
	点字ディスプレイと点字キーボード
	locale LANG=Cで英語
	crontab 0,7日曜日 
	useradd UID 可能なminのUID 
		GID 同じ名前IDのグループ割りあて
	SPICE 4画面マルチモニタ、ネットワーク経由USB、
	ホスト上
	VNC ゲストOS上
	bash 設定ファイル
		シェルスクリプト
		sourceコマンドで実行してる ディレクトリxとシェルスクリプトのr
	~/.bash_profile, ~/.bash_login ~/.profileから/.bashrcが読みこまれる
	passwd,usermod -p でパスワード変更
	ssh-agent bash 
		ssh-agentがbashをバックグラウンド起動 
		そのシェルでssh,ssh-addする
	sendmail コマ 引数無し 標準入力受けつけ .ピリオドまで
	nmcli 
		device ハードそのもの 個別スイッチ
		connection 接続の設定プロファイル
		networking OSのnetwork全体のOnOff
	dig 最初の行に問い合わせコマンド
	ifup,ifdown 設定ファイル　各ディストリビューションで異なる
	ネットワークインターフェースの設定ファイルを読みこんで起動　変更しない
	/etc/crontab 実行ユーザー指定 システム
	/var/spool/cron ユーザー
	xサーバーはキーボードから入力受付して描画してる
	DISPLAY変数　ホスト名はXサーバーを各 Xクライアント側に変数持つ
	描画先を指定する変数
	DNS 
		dig,host-v nslookup
	エイリアス
		bashrc系
	環境設定
		bash_profile系
	/etc/shadow/ 000 400 600
	CUPS設定　/etc/cups.conf
	cupd設定 /etc/cups/cupsd.conf
	プリンター設定/etc/cups/printers.conf
	ip route del アドレス via ゲートウェイ dev インターフェース名
	DNS
		searchで名前解決しなかったらnameserverの一番上から検索する
	systemd-journald 
		syetem.journalというジャーナルファイル記録　バイナリ
		/run/log/journalで一時記録
	CUPS標準コマンド
		lpstat cancel lp 
	IPのクラス
	+128 +64してけばいい
	NetworkManager 特定のインタフェースを管理
	管理しないやつも指定できる 
	fuser -u -v ユーザー指定
		-m マウントFS
		-n 検索領域指定
	dig any
	ulimit -u ユーザー使えるプロセス数
	CUI→GUI startx
	Xsetup_0 ログイン前に実行される カラー設定とか壁紙
	exec そのシェルで実行
	ユーザーの切り替えって子シェル作ってる
	getent /etc/nsswitchを参照する
	IP↔ホスト名
		host,dig,nslookup,getent
	CUPS
		IPP ネットワークプリンターサポート
		PPD プリンタの機種依存設定記述
		webブラウザ
		ipd互換
	xサーバー許可
		+ホスト名 +省略可能 許可
		-ホスト名　削除
		+ アクセス制限off みんなok
		- アクセス制限ON やっぱ書いてるやつだけ
	batchもat.allow適用
	リピートキー
	MTA postfix 標準
	journalctl less
	一括表示->ページャない
	xinet socket_type サービスの接続タイプ
		wait マルチ off シングル on
	ntpd ntpq
	エポック時間　　1970年からの時計
	gpasswd グループパス、メンバー管理
	chrony activity NTPサーバーオンライン
		sources 時間のソース
		tracking
	nmcli
		 device connect/disconnect
		 connection up/down
		 networking on/off
	gpg-agent パスフレ認証状態をキャッシュ
	nmcli 問題起きたらエラーかえす
	syslog は設定しないとsystem-journaldから読みだセナい
	rsyslogはおっけ
	jounarld syslogへのログも記録する
	




	
	
・感想
	がっつり出来てヨシ
・次やること
	pingt