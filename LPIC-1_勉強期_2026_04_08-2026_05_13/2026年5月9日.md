・勉強時間
	2.5h
・内容
	LPIC 102 ping 150問　残137問
・学び
	ntpq -p でserverの同期一覧 peer
	ntpdate 手動でNTPと時刻同期
	IP　ネット機器のアドレス付与、パケット分割、統合
	ICMP TTL0になったら返す
	ifconfig
		IP、ブロードキャスト、サブネットマスク、MAC、MTU
	at 23:00の書き方
	at -l atq ジョブ一覧
	at -d atrm ジョブ削除
	xinetd アクセス制御
		/etc/xinetd.d/内の個別ファイルで設定
		利用頻度の低いサービス要求
	-perm -xxxx AND検索,  /xxxx OR検索どれか一つ , xxxx 完全一致
	 /etc/default/useradd デフォ環境変数格納されてる
	 env -i ignore 環境変数なんもなし
		 -u unset 一時的環境変数なし
		 EV=値 環境変数一時的指定
	userdel -r remove ユーザーのホームディレクトリ削除
	-f ログイン中でも
	case 値 in
		パターン1) 実行;;
	esac
	= 文字列等しい -eq 数値等しい
	journal -n or --lines デフォ10
	ip link ネットワークデバイス
		ND = NI 実質一対一
		ND 物理 NICとか
		NI IPとかMAC設定
	xinetd instances デーモンの最大起動プロセス数
	Netcat　簡易的通信、簡易的サーバー確認
	nc -l listen
		-p port 送信元ポート指定
		-u udp デフォのTCP出る
	$1-$9 全部一個ずつ入る
		readは最後まとめて入る
	xfce　軽量
	chrony server クラサバ　一方向
		peer 同じstartum 双方向
	 v4 ->v6 消えたフィールド残る
	 LC_NAME LC_ADDRESS LC_TELEPHONE
	 LANG 全カテゴリ上書き 個別可能
		 LC_ALL 全カテゴリ上書き　個別不可　非推奨
	 su 切り替わる先のユーザーのパス
	 sudo 実行するユーザーのパス
	 netstat lsof ローカルホストのポート確認
	 nmap リモートホストのポートスキャン
	 chage -l パスの有効期限に関する情報表示
	 tzconfig /etc/localtime /etc/timezone まとめて設定
	 tzselect 環境変数TZ /etc/timezone ファイルで指定する値の確認
	 journalctl -l ,--full
	 xinetd bind or interface サービス提供のインタフェースIP表示
		 サービスがどのIPのみ受付するか制限
	 /etc/chrony.conf rtcsync シスクロをrtcに適用
	 socketユニット スーパーサーバーと同じ
	 journalctl -xb エマージェンシーモードで確認するやつ -x 追加情報
・感想
	良い速度でpingt出来た
	大分脳の中で知識が圧縮されてきたのを感じる
・次やること
	pingt終わらせる
	テスト日決める
	セッティング