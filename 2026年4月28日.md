・勉強時間
	5.5h
・内容
	LPIC 102 あずき本11章　ネットワーク
・学び
	IPv6の省略記法
		プレフィックスとインターフェースID
	リンクローカルアドレス(LLA)
		内での通信
	グローバルユニキャストアドレス(GUA)
		外との通信 外向けのIPv6のアドレス
	/3と/64
		/3はインターネット全体での予約範囲　先頭001....
		/64は個別ネットワークの区切り
	ポート
		/etc/serveces
		ウェルノウンポートは出る！
	ネットワーク設定
		/etc/hostname
		/etc/hosts
		/etc/network/interfaces
		/etc/sysconfig/network-scripts
	ネットワークインターフェースとは
		通信用のOSが認識した論理的な窓口
	NetworkManager
		nmcliコマンドで設定
	トラブルシューティング
		ping 
			ICMPパケット
		traceroute
		tracepath
		hostname
		netstat (ss)
		nc (netcat)
	ゲートウェイ
		中継地、受付係　単なる役割語
		主にルーター、L3スイッチ、サーバー、ファイアウォールとか
	ルーティングテーブル
		/proc/sys/net/ipv4/ip_forward が1か
		Flagsに注意
			U,H,G
		ipコマンド
			色々とまとめたコマンド
	ネットワークインターフェース
		ifconfigコマンド
		再起すると失われる　永続は/etc以下に書き込め
		ifup ifdown
	DNS
		ホスト名→IP　正引き　逆　逆引き
		ホスト名とドメイン名
		/etc/resolv.conf
			参照するDNSキャッシュサーバーを指定
		/etc/nsswitch.conf
			名前解決の時に/etc/hostsを使うかDNSを使うか優先順位
		DNSサーバー
			DNSキャッシュサーバー
				パシリ、検索してくるやつ、直近のドメイン名とか覚えておく(キャッシュ)
			DNSコンテンツサーバー
				パシリに回答するやつ、情報持ってるやつ
		systemd
			参照先DNS書いてる
			/etc/systemd/resolve.conf
		管理コマンド
			host 簡易的
			dig 詳細
			レコードタイプ覚える
・感想
	今回も丁寧に
	既存のネットワークについて学んでいたことが繋がった感覚があって気持ち良い
	思ったより丁寧になった
・次やること
	あずき本終わらせ 12章