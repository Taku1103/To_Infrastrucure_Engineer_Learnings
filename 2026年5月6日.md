・勉強時間
	5.5h
・内容
	LPIC 102 ping-t 0->120/655 
・学び
	getent passwd　情報収集
	Network Manager
		DHCP 動的IP割り当て
		ユーザー設定重視
	v6 ホップリミット = v4 TTL
	ルーティングテーブル
		route 
		netstat -r
		ip route showちょっと違う表記
	startx ->xinit
	ifconfig->ip
	logger syslog生成
	system-cat system-journald
	${10}で{}で囲む必要あり
	インタープリターとしてシェルを使う
		シェルをプログラミング実行エンジンとして利用
	xauth ユーザー単位でXサーバーへのアクセス制限
	nmcli オプション
	/etc/chrony.conf
	ssh-agent 環境変数 SSH_AUTH_SOCK使う
	プラ
		172.16.0 -172.31.255
		192.168.
		10
	journalctl　オプション
	fuser -k killできる
	/etc/group プライマリーグループ以外
	. or source 
	sh bash シェルで実行
	lsof -i:ポート番号
	Netcat nc 標準入出力データ授受
	環境変数DISPLAY 
		sshを使用してX11転送が成功した場合、リモートホストのシェル環境で自動で設定
・感想
	復活!
	ここからリズムを取り戻していく
・次やること
	